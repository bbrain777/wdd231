export function setTitle(course){const h=document.querySelector('#courseTitle');if(h)h.textContent=`${course.code} — ${course.name}`;}export function renderSections(sections){const out=document.querySelector('#sectionsOutput');out.innerHTML=sections.map(s=>`<div class="card"><div class="name">Section ${s.sectionNum}</div><div>${s.days} · ${s.time} — Room ${s.roomNum}</div><div>Enrolled: <strong>${s.enrolled}</strong></div></div>`).join('');}